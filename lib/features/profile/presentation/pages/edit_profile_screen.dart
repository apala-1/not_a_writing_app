import 'package:flutter/material.dart';

class EditProfileScreen extends StatelessWidget {
  const EditProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // pfp change 

      // name change (only once every 30 mins)

      // profession change

      // bio change

      // email change

      // password change
    );
  }
}